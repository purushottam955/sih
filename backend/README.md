# Backend for Skill Intelligence Platform (SIH 2026)

## Setup
1. Install dependencies: `npm install`
2. Copy `.env.example` to `.env` and fill in database URL and JWT secret.
3. Run migrations: `npm run migrate`
4. Seed database: `npm run seed`
5. Start server: `npm start`

## API Endpoints
See the detailed list in the backend documentation.